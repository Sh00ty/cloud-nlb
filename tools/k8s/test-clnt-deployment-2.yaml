apiVersion: apps/v1
kind: Deployment
metadata:
  name: testclient-2
  namespace: cloud-nlb
spec:
  replicas: 1
  selector:
    matchLabels:
      app: testclient-2
  template:
    metadata:
      labels:
        app: testclient-2
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8081"
        prometheus.io/path: "/metrics"
    spec:
      containers:
        - name: testclient
          image: testclient:dev
          args:
            - --url=http://nlb-vip-test-server.cloud-nlb:10001/ping
            - -c=10
            - --qps=50
            - --timeout=200ms
            - --keepalive=false
            - --metrics-addr=0.0.0.0:8081
          ports:
            - containerPort: 8081
              name: metrics
