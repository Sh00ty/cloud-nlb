apiVersion: v1
kind: Service
metadata:
  name: control-plane
  namespace: cloud-nlb
  labels:
    app: control-plane
spec:
  type: ClusterIP
  ports:
  - name: grpc
    port: 9090
    targetPort: grpc
    protocol: TCP
  selector:
    app: control-plane