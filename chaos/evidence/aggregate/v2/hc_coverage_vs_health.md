# Интервал покрытия проверками против отказа узлов HC

Источник: замороженные `metrics/avail__cov_p90_15s.json` (резкое окно `rate[15s]`) и `metrics/avail__live_hc.json` по всем прогонам, офлайн. Узлов HC недоступно = `max(live_hc) - live_hc`, `live_hc = count(up{app="hc-worker"}==1)` (StatefulSet из 4 реплик).

**Оговорка о разрешении.** Интервал покрытия снят как `histogram_quantile(0.9, rate(coverage_bucket[15s]))`. При скрейпе ~5 с окно `rate[15s]` это ~3 выборки, в ~8 раз резче штатного графика `rate[2m]`. Это вскрывает главное: реакция интервала на отказ узлов HC ступенчатая, а не плавная. Прежний плавный подъём под `rate[2m]` (с ~2.8 до ~3.4 с за ~100 с) был артефактом усреднения метрики, а не разгоном системы: на резком ряду интервал переходит с базы на полку за один скрейп. Разрешающая способность по времени ограничена снизу величиной скрейп ~5 с плюс окно `rate` 15 с, провалы короче не различимы. Окно `rate[10s]` тоже заморожено (`avail__cov_p90_10s.json`), но на 2 выборках шумит (выброс на переходе), поэтому головным взят `[15s]`. Более резкого серверного сигнала нет: датчик `testserver_hc_time_since_last_request_seconds` тождественно нулевой во всех прогонах (сборкой тестового сервера на стенде не экспонируется) и сознательно не используется. Счётчик упавших узлов HC снят со скрейпом 10 с: при быстрых циклах kill/restart пода (фазы 2–4) часть провалов `up` между скрейпами не видна, поэтому «макс. узлов HC down» в комбинированных фазах это нижняя оценка; чисто число снято только в `single-hc-failure-long` (3 из 4 узлов недоступны непрерывно ~4 мин). База это медиана нижней половины p90 за прогон (устойчивый уровень между сценариями).

| Логический сценарий | База p90, с | Пик p90, с | Реакция (отказ→полка), с | Макс. узлов HC down | Восст. p90 после возврата узлов, с |
|---|---|---|---|---|---|
| single-hc-kill (убийство 3/4, кратко) | медиана 2.4 (мин 1.8 — макс 2.6) | медиана 3.1 (мин 2.7 — макс 5.5) | медиана 0.0 с (мин 0.0 — макс 8.0 с) | медиана 2.0 (мин 1.0 — макс 3.0) | медиана 4.0 с (мин 0.0 — макс 88.0 с) |
| single-hc-failure-long (отказ 3/4, 4 мин) | медиана 2.4 (мин 1.8 — макс 2.6) | медиана 3.3 (мин 2.8 — макс 7.7) | медиана 5.0 с (мин 0.0 — макс 12.0 с) | медиана 2.5 (мин 1.0 — макс 3.0) | медиана 14.0 с (мин 0.0 — макс 16.0 с) † |
| HC + 1 отказ (парные c hc) | медиана 2.4 (мин 1.8 — макс 2.6) | медиана 3.0 (мин 2.6 — макс 3.5) | медиана 4.0 с (мин 0.0 — макс 186.0 с) | медиана 2.0 (мин 1.0 — макс 3.0) | медиана 0.0 с (мин 0.0 — макс 16.0 с) † |
| HC + 2 отказа (тройные c hc) | медиана 2.4 (мин 1.8 — макс 2.6) | медиана 2.9 (мин 2.5 — макс 8.2) | медиана 2.0 с (мин 0.0 — макс 10.0 с) | медиана 1.0 (мин 0.0 — макс 3.0) | медиана 0.0 с (мин 0.0 — макс 18.0 с) † |
| HC в тотальном хаосе | медиана 2.4 (мин 1.8 — макс 2.6) | медиана 6.2 (мин 2.5 — макс 10.0) | медиана 34.0 с (мин 0.0 — макс 208.0 с) | медиана 1.0 (мин 0.0 — макс 4.0) | медиана 0.0 с (мин 0.0 — макс 66.0 с) † |

Реакция это время от падения первого узла HC до выхода p90 на деградированную полку (половина пути база→пик); на резком ряду это порядка одного скрейпа. Восстановление это время от возврата всех узлов HC до возврата p90 в полосу база+0.5 с с удержанием 30 с; 0.0 означает, что p90 из полосы базы не выходил.

† в части прогонов p90 не успел вернуться в полосе базы до старта следующего сценария: окно наблюдения после возврата узлов короче ~25 с (характерно для `single-hc-failure-long`, где зазор до следующего сценария всего ~21 с). Это ограничение наблюдения, заданное расписанием набора, а не медленное восстановление системы.

## Итог

На резком ряду видно, что при потере 3 из 4 узлов HC интервал покрытия мгновенно (за один скрейп) переходит с ~2.8 с на ровную полку ~3.4 с и держит её, не нарастая, всю недоступность; запас до бюджета обнаружения DOWN 9 с порядка 5.5 с. Реакция системы это шаг, а не разгон. Время восстановления интервала измеримо там, где окно наблюдения после возврата узлов достаточно длинное; для самого долгого сценария оно упирается в зазор расписания ~21 с, поэтому для него восстановление подаётся как ограниченное наблюдением, а не как свойство системы. В тотальном хаосе по типичному прогону p90 ≈ 2.3 с (запас более 6 с), в одном худшем прогоне кратковременный выброс до ~9–10 с (упор в потолок метрики 10 с) приводится как граница, не типичный режим.
